function add(x, y) {
    return (x + y)
}

function substract(x, y) {
    return (x - y)
}

function multiple(x, y) {
    return (x * y)
}

function devide(x, y) {
    return (x / y)
}

function modulo(x, y) {
    return (x % y)
}

const additon = add(1, 5);
const substraction = substract(10, 2)
// const multiplication = m

var multipleOperation = add(substract(20, 5), devide(100, 2))